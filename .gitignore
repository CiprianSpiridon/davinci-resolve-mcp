# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment / secrets
.env

# OS
.DS_Store

# Logs
*.log

# Session tooling / installed skills (not part of this project) — but DO ship
# this project's own dedicated Claude Code skill under .claude/skills/davinci-resolve/.
.agents/
.claude/*
!.claude/skills/
.claude/skills/*
# Ship the symlinks that mirror this project's own skills into the Claude Code
# skills dir (canonical sources are the root-level davinci-resolve* skill dirs).
!.claude/skills/davinci-resolve
!.claude/skills/davinci-resolve-remove-silences-bad-takes-and-umms
!.claude/skills/davinci-resolve-use-plugins
!.claude/skills/davinci-resolve-generate-plugin-list
skills-lock.json

# Local Ulpi artifacts
.ulpi/

# Machine-specific generated plugin TOC + control dumps (davinci-resolve-generate-plugin-list
# output — reflects THIS machine's installed packs; regenerate per machine, don't ship).
davinci-plugins.json
davinci-plugins.md
davinci-plugin-controls.json
davinci-plugin-controls.md
CLAUDE.md
